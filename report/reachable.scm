(define (a) 'a)
(define (b)
  (define (d)
    (define (l) 'l)
    'd)
  (define (e)
    (define (f)
      (define (h) (l))
      (define (i) 'i)
      (d)
      (h))
    (define (g)
      (define (j) 'j)
      'g)
    (f))
  (e))
(define (c)
  (define (k) 'k)
  'c)